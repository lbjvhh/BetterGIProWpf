<Page x:Class="BetterGIProWpf.Pages.CoachPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource BgMain}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="实时策略教练 · 视频对照" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource TextMain}"/>
        <Border Grid.Row="1" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource Border}"
                BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,12,0,12">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="建议频率" Width="90" VerticalAlignment="Center"/>
                    <ComboBox x:Name="FreqCombo" Width="180" SelectedIndex="2">
                        <ComboBoxItem Content="关闭" Tag="Off"/>
                        <ComboBoxItem Content="低频" Tag="Low"/>
                        <ComboBoxItem Content="正常" Tag="Normal"/>
                        <ComboBoxItem Content="高频" Tag="High"/>
                    </ComboBox>
                    <TextBlock Text="  触发阈值" Width="80" VerticalAlignment="Center"/>
                    <Slider x:Name="ThreshSlider" Minimum="0.1" Maximum="0.9" Value="0.35" Width="160"/>
                    <TextBlock x:Name="ThreshLabel" Text="0.35" Width="50" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextMuted}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button Content="▶ 模拟一次对比分析" Width="150" Click="Analyze_Click"/>
                    <CheckBox x:Name="VoiceChk" Content="语音播报建议" IsChecked="True" Margin="16,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="  建议类型：元素反应 / 体力管理 / 路线优化 / 技能时机 / 敌人弱点（≥5 种）"
                               Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>
        <Border Grid.Row="2" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="8">
            <TextBox x:Name="CoachLog" IsReadOnly="True" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12"
                     Background="White" BorderThickness="0" Padding="10" VerticalScrollBarVisibility="Auto"/>
        </Border>
    </Grid>
</Page>
