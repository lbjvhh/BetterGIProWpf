<Page x:Class="BetterGIProWpf.Pages.RecognitionPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Background="{DynamicResource BgMain}">
  <Grid Margin="24"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <TextBlock Text="Recognition Layer" FontSize="20" FontWeight="SemiBold"/>
    <Border Grid.Row="1" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,12,0,12"><StackPanel>
      <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
        <Button Content="Multi-scale Match" Width="130" Margin="0,0,8,0" Click="Template_Click"/>
        <Button Content="DTW Align" Width="100" Margin="0,0,8,0" Click="Dtw_Click"/>
        <Button Content="Overlay->Script" Width="140" Margin="0,0,8,0" Click="Overlay_Click"/>
        <Button Content="Channel B" Width="110" Margin="0,0,8,0" Click="ChannelB_Click"/>
        <Button Content="NitroGen Bridge" Width="130" Margin="0,0,8,0" Click="Bridge_Click"/>
        <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="{DynamicResource TextMuted}"/>
      </StackPanel>
    </StackPanel></Border>
    <Border Grid.Row="2" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="8"><TextBox x:Name="ResultText" IsReadOnly="True" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" Background="White" BorderThickness="0" Padding="10"/></Border>
  </Grid>
</Page>
