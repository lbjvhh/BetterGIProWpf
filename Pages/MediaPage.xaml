<Page x:Class="BetterGIProWpf.Pages.MediaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource BgMain}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="高光剪辑 · 战报生成 · 解说配音" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource TextMain}"/>
        <Border Grid.Row="1" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource Border}"
                BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,12,0,12">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="成片时长" Width="90" VerticalAlignment="Center"/>
                    <ComboBox x:Name="LenCombo" Width="140" SelectedIndex="0">
                        <ComboBoxItem Content="60 秒" Tag="60"/>
                        <ComboBoxItem Content="90 秒" Tag="90"/>
                        <ComboBoxItem Content="120 秒" Tag="120"/>
                    </ComboBox>
                    <TextBlock Text="  比例" Width="60" VerticalAlignment="Center"/>
                    <ComboBox x:Name="RatioCombo" Width="100" SelectedIndex="0">
                        <ComboBoxItem Content="16:9"/>
                        <ComboBoxItem Content="21:9"/>
                    </ComboBox>
                    <Button Content="▶ 模拟录制+高光识别" Width="160" Margin="12,0,0,0" Click="Record_Click"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button Content="生成战报统计" Width="130" Click="Report_Click"/>
                    <Button Content="生成解说词+SRT" Width="140" Margin="8,0,0,0" Click="Narrate_Click"/>
                    <TextBlock x:Name="MediaStatus" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{DynamicResource TextMuted}"/>
                </StackPanel>
            </StackPanel>
        </Border>
        <Border Grid.Row="2" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="8">
            <TextBox x:Name="MediaLog" IsReadOnly="True" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12"
                     Background="White" BorderThickness="0" Padding="10" VerticalScrollBarVisibility="Auto"/>
        </Border>
    </Grid>
</Page>
