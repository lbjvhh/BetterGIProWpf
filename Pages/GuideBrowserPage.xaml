<Page x:Class="BetterGIProWpf.Pages.GuideBrowserPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" Background="{DynamicResource BgMain}">
  <ScrollViewer Padding="24"><StackPanel>
    <TextBlock Text="Built-in Guide Browser" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12"/>
    <Grid Margin="0,0,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <TextBox x:Name="UrlBox" Grid.Column="0" Height="34" VerticalContentAlignment="Center" Text="https://www.bilibili.com" Margin="0,0,8,0"/>
      <Button x:Name="OpenBtn" Grid.Column="1" Content="Open" Width="80" Height="34" Click="OpenBtn_Click"/>
    </Grid>
    <Border Height="440" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="8"><wv2:WebView2 x:Name="Web"/></Border>
    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
      <Button Content="Play" Width="80" Margin="0,0,8,0" Click="PlayBtn_Click"/>
      <Button Content="Pause" Width="80" Margin="0,0,8,0" Click="PauseBtn_Click"/>
      <Button Content="Capture Frame" Width="110" Margin="0,0,8,0" Click="CaptureBtn_Click"/>
      <Button Content="Analyze Video" Width="120" Margin="0,0,8,0" Click="AnalyzeBtn_Click"/>
      <Button Content="Go Execute" Width="100" Margin="0,0,8,0" Click="GoExecuteBtn_Click"/>
      <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="{DynamicResource TextMuted}"/>
    </StackPanel>
    <Border Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,12,0,0"><TextBlock x:Name="ResultText" TextWrapping="Wrap"/></Border>
  </StackPanel></ScrollViewer>
</Page>
