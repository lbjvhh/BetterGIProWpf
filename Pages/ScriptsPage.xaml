<Page x:Class="BetterGIProWpf.Pages.ScriptsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource BgMain}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="User 目录:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox x:Name="UserRootBox" Width="360" Height="26" Text="C:\BetterGI\User" VerticalContentAlignment="Center" Margin="0,0,8,0"/>
                <Button Content="扫描" Click="Scan_Click" Width="70" Height="26" Margin="0,0,8,0"/>
                <Button Content="执行脚本" Click="RunScript_Click" Width="90" Height="26" Margin="0,0,16,0"/>
                <Button Content="异常修复" Click="FixAnomaly_Click" Width="90" Height="26" Margin="0,0,8,0"/>
                <TextBox x:Name="AnomalyBox" Width="160" Height="26" Text="getPositionFromMap 返回 null" VerticalContentAlignment="Center" Margin="0,0,8,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="卡死/转圈检测" Click="Stuck_Click" Width="120" Height="26" Margin="0,0,8,0"/>
                <Button Content="仓库刷新" Click="RepoRefresh_Click" Width="90" Height="26" Margin="0,0,8,0"/>
                <TextBox x:Name="RepoSearchBox" Width="180" Height="26" VerticalContentAlignment="Center" Margin="0,0,4,0"/>
                <Button Content="搜索仓库" Click="RepoSearch_Click" Width="90" Height="26" Margin="0,0,8,0"/>
                <Button Content="订阅脚本" Click="RepoSubscribe_Click" Width="90" Height="26" Margin="0,0,16,0"/>
                <TextBlock Text="拟人强度:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox x:Name="HumanCombo" Width="100" Margin="0,0,8,0">
                    <ComboBoxItem Content="低" Tag="Low"/>
                    <ComboBoxItem Content="中" Tag="Medium" IsSelected="True"/>
                    <ComboBoxItem Content="高" Tag="High"/>
                </ComboBox>
                <Button Content="拟人化分析" Click="Humanize_Click" Width="100" Height="26" Margin="0,0,8,0"/>
                <Button Content="UI 适配" Click="UiAdapt_Click" Width="90" Height="26" Margin="0,0,8,0"/>
                <Button Content="网络切换" Click="Network_Click" Width="90" Height="26"/>
            </StackPanel>
        </StackPanel>
        <TextBox x:Name="ScriptsLog" Grid.Row="1" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" FontFamily="Consolas" Margin="0,8,0,0"/>
    </Grid>
</Page>
